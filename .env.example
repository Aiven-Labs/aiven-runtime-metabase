# Local demo only. Choose unique values; never commit .env.
POSTGRES_PASSWORD=
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=
# Generate once using: openssl rand -base64 32
# Keep this key unchanged across restarts and back it up separately from PostgreSQL.
MB_ENCRYPTION_SECRET_KEY=
